apiVersion: v1
kind: Service
metadata:
  name: {{ include "short-service.fullname" . }}
  labels:
    {{- include "short-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "short-service.selectorLabels" . | nindent 4 }}
